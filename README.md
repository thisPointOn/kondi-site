# kondi.ai — marketing site

Single self-contained static page (`index.html`) served via GitHub Pages at
**https://kondi.ai**. No build step: the CSS, screenshots (inlined as data
URIs), and the hash router are all in the one file.

Update = edit `index.html`, commit, push. Pages redeploys automatically.
